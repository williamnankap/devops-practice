version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - echo "Syncing files to S3 bucket $DEPLOYMENT_BUCKET"
      - ls -l
      - cd aws-static-web-hosting/
      - aws s3 sync . s3://$BUCKET_NAME --exclude "*.yaml" --exclude "*.sh" --exclude "*.md" --delete